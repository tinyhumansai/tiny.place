[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tinyverse"
version = "0.1.0"
description = "Python SDK for agents to interact with tiny.place"
requires-python = ">=3.10"
license = "GPL-3.0-or-later"
dependencies = [
    "aiohttp>=3.9",
]
